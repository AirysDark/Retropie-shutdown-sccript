#!/bin/sh
# Retroflag Pi Case shutdown script for OSMC
# This script is executed by service.py when a case button is pressed.

button="$1"
case "$button" in
    power)
        # poweroff
        shutdown -h now
        ;;

    reset)
        # reboot
        reboot
        ;;
esac
